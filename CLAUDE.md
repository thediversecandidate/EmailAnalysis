# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

An empty repository — the only file is a broken autogenerated
`README.md` (its `$repo` template variable was never substituted). There is
no source code, dependency manifest, or test setup yet. Despite the name,
there's no NLP/email-parsing implementation here to build on; if asked to
build "the email analysis tool," this is a from-scratch start, not a
partially-built one.
